import postgresSourceStyles from "./postgresSource.css";

/**
 * How PostgreSQL source reads: line numbers, token colours, the frame around it. A bundle that
 * links its stylesheets names the file; one that inlines them takes this.
 */
export { postgresSourceStyles };
