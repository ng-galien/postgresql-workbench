services:
  postgres:
    build:
      context: ../..
      dockerfile: demo/Dockerfile
    image: postgresql-workbench-postgres:pg17
    ports:
      - "${PGWB_BENCH_PORT:-55433}:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
